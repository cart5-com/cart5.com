{
  "name": "proxy-caddy",
  "scripts": {
    "dev": "pnpm format && dotenv -e ../../.env -- caddy run --config ./Caddyfile_dev",
    "preview": "pnpm dev",
    "format": "caddy fmt ./Caddyfile_dev  --overwrite"
  }
}