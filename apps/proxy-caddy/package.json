{
  "name": "proxy-caddy",
  "scripts": {
    "dev:caddy": "pnpm format && dotenvx run -- caddy run --config ./Caddyfile_dev",
    "format": "caddy fmt ./Caddyfile_dev  --overwrite",
    "caddy:deploy": "dotenvx run -f .env.production --  bash -c 'cd ./deploy && ./_start.sh'"
  },
  "dependencies": {
    "@dotenvx/dotenvx": "^1.32.0"
  }
}