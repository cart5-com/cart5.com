---
import BaseLayout from "@web-astro/layout/BaseLayout.astro";
interface Props {
	error: unknown;
}

// const { error } = Astro.props;
---

<BaseLayout title="500">
	<h1>500 Internal Server Error</h1>
	<!-- <div>{error instanceof Error ? error.message : "Unknown error"}</div> -->
</BaseLayout>
