# Auth Client (Astro SSG)

Static authentication client interface built with Astro, providing a seamless authentication experience.

## Features

- Static site generation
- OTP authentication flow
- Cross-domain session handling

Note: We may need these dependencies if we create some components with ui-shadcn-vue components inside this astro app.

```
"dependencies": {
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-vue-next": "^0.465.0",
    "radix-vue": "^1.9.10",
    "tailwind-merge": "^2.5.5",
    "tailwindcss-animate": "^1.0.7"
}
```
